apiVersion: apps/v1
kind: Deployment
metadata:
  name: marketcrawler
  annotations:
    configuration/period: "{{ .Values.period }}"
    configuration/transmittergroup: "{{ .Values.dapnet.transmitterGroupName }}"
    configuration/errorwaittime: "{{ .Values.errorwaittime }}"
    configuration/telegramtoken: {{ .Values.telegram.telegramToken }}
  labels:
    app: marketcrawler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: marketcrawler
  template:
    metadata:
      labels:
        app: marketcrawler
    spec:
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: "Always" 
      restartPolicy: Always
      containers:
        - name: marketcrawler
          image: {{ .Values.marketcrawlerimage }}
          imagePullPolicy: Always
          env:
            - name: blogurl
              value: {{ .Values.blogurl }}
            - name: telegramToken
              value: {{ .Values.telegram.telegramToken }}
            - name: telegramChannel
              value: "{{ .Values.telegram.telegramChannel }}"
            - name: dapnet
              value: {{ .Values.dapnet.dapnetID }}
            - name: transmitterGroupName
              value: {{ .Values.dapnet.transmitterGroupName }}
            - name: callsign
              value: {{ .Values.dapnet.callsign }}
            - name: envname
              value: {{ .Values.envname }}
            - name: period
              value: "{{ .Values.period }}"
            - name: errorwaittime
              value: "{{ .Values.errorwaittime }}"
